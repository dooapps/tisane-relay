# --- Relay Configuration ---
# Public URL for your relay (used by Caddy for TLS)
DOMAIN_NAME=relay.example.com
# Email for Let's Encrypt notifications
ACME_EMAIL=admin@example.com

# Internal Port (mapped to 8080 in container, exposed via Caddy on 80/443)
PORT=8080

# Unique ID for this relay.
# REQUIRED: Generate via `uuidgen` and KEEP IT CONSTANT.
# If this changes, your peers will treat you as a new relay and replication state may reset.
RELAY_ID=

# --- Database Configuration ---
POSTGRES_USER=tisane_admin
POSTGRES_PASSWORD=change_me_please
POSTGRES_DB=tisane_relay

# --- Logging ---
RUST_LOG=info
